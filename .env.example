# Copy to .env (which is gitignored). Never commit real values.

# Required only for `screener cluster` (Stage 5).
ANTHROPIC_API_KEY=

# Goes into the User-Agent so Apple can identify the crawler if it ever matters.
OPERATOR_CONTACT=you@example.com
